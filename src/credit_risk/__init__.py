"""Lauki Finance credit risk scorecard.

A framework-free core (``domain`` + ``scoring``) with a Streamlit presentation
layer (``ui``), so the same scoring engine can back the SOW Phase 2
Straight-Through-Processing service without a rewrite.
"""

from __future__ import annotations

__version__ = "1.0.0"
